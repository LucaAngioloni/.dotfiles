# shellcheck disable=SC2148

# Set personal aliases, overriding those provided by oh-my-zsh libs,
# plugins, and themes. Aliases can be placed here, though oh-my-zsh
# users are encouraged to define aliases within the ZSH_CUSTOM folder.
# For a full list of active aliases, run `alias`.

alias npmup='npm install npm -g; npm update -g;'
alias rubyup='sudo gem update --system; sudo gem update; sudo gem cleanup'

# youtube-dl is no longer maintained, use yt-dlp instead
# alias ydl='youtube-dl'
# alias ydl-a='youtube-dl --ignore-errors --output "%(title)s.%(ext)s" --extract-audio --audio-format mp3'
# alias youtube-dl-audio='youtube-dl --ignore-errors --output "%(title)s.%(ext)s" --extract-audio --audio-format mp3'

alias ydl='yt-dlp'
alias ydl-a='yt-dlp -f bestaudio -x --audio-format mp3'

# Not needed, use esc twice with the thefuck ohmyzsh plugin
# eval "$(thefuck --alias)"

# alias aaa='fuck'

alias nom='npm' # Because I always type it wrong
alias npmg="npm list -g --depth=0 2>/dev/null"
alias npml="npm list --depth=0 2>/dev/null"

alias jupyter-lab='jupyter lab --no-browser --ip "*"'

alias ncdu='ncdu --color dark'

alias dsrm='find . -name ".DS_Store" -type f -delete'
alias pycrm='find . -name "__pycache__" -type d -delete'

alias clone='git clone'

alias code='vscode'